# Terraform Infrastructure Setup

## Pre-requisites
- Terraform >=1.0
- Azure CLI installed
- Logged in to Azure (`az login`)

## How to Deploy

```bash
cd terraform/
terraform init
terraform plan
terraform apply

